flowchart TD
    beginning(début)
    --- writeInputA[/écrire 'saisir A'/]
    --- readA[/lire A/]
    --- writeInputB[/écrire 'saisir B'/]
    --- readB[/lire B/]
    --- assignTemp[temp = a]
    --- assignA[a = b]
    --- assignB[b = temp]
    --- writeResultA[/écrire A/]
    --- writeResultB[/écrire B/]
    --- ending(fin)